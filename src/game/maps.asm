; Phase-1 asset banks moved the authored map pool out of stage two.
; The readable map source still builds to build\generated_maps.inc for review,
; but gameplay now reads the raw payload from MAP_BANK_SEG via
; template_offset_table in generated_sector_content.inc.
